{
  "name": "til",
  "version": "1.0.0",
  "description": "bong-u's til blog with hugo",
  "scripts": {
    "prestart": "rm -rf static/pagefind",
    "start": "hugo server --bind '0.0.0.0' --port=1313 --disableFastRender",
    "update-pagefind": "hugo --minify -b 'http://localhost:1313/' && npx pagefind --site public && cp -r public/pagefind static/",
    "watch": "npx @tailwindcss/cli -i ./static/css/main.css -o ./static/css/style.css --watch"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bong-u/til.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/bong-u/til/issues"
  },
  "homepage": "https://blog.bong-u.com",
  "devDependencies": {
    "@tailwindcss/cli": "^4.2.2",
    "hugo-extended": "^0.161.1",
    "tailwindcss": "^4.2.2"
  },
  "dependencies": {
    "@google-analytics/data": "^5.2.1"
  }
}
